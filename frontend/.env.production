# Production build. Deployed backend URL (no trailing slash).
REACT_APP_BASE_URL=https://chatapp-3oy9.onrender.com

# CRA treats ESLint warnings as errors when CI=true (Vercel/Render set this).
# Disable the build-time ESLint plugin so harmless warnings don't fail the build.
DISABLE_ESLINT_PLUGIN=true
